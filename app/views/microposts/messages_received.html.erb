<% if signed_in? %>
<% @messagesreceived = messagesReceived() %>
<h1>Messages Received (Last 200)</h1>
<table class="table table-bordered table-hover table-condensed" id="messages-received" >  
 <!--  <table class="display" id="messagessent" > -->
    <thead>
      <tr>
        <td>SID</td>
        <td>Date Created</td>
        <td>Date Updated</td>
        <td>Date Sent</td>
        <td>From</td>
        <td>Message</td>
        <td>Status</td>
      </tr>
    </thead>
    <tbody>   
      <% @messagesreceived.each do |thismessage| %>
        <tr>                                     
          <td><%= thismessage.sid %></td>  
          <td><%= thismessage.date_created %></td> 
          <td><%= thismessage.date_updated %></td> 
          <td><%= thismessage.date_sent %></td> 
          <td><%= thismessage.from %></td> 
          <td><%= thismessage.body %></td> 
          <td><%= thismessage.status %></td> 
        </tr>
      <% end %>
    </tbody>
  </table>
  <script>
  $(document).ready(function() {
    $('#messages-received').dataTable({
        "sDom": "<'row-fluid'<'span6'l><'span6'f>r>t<'row-fluid'<'span6'i><'span6'p>>",
        "sPaginationType": "bootstrap",
        "aaSorting": [[ 3, "desc" ]],
        "iDisplayLength": 50
    });
  } );

</script>

<% end %>
